# TI Aggregator — Python Dependencies
# Install with: pip install -r requirements.txt

# HTTP feed fetching
requests>=2.28.0

# All other dependencies (re, json, csv, ipaddress, collections,
# datetime, os, sys) are part of the Python standard library
# and require no installation.
